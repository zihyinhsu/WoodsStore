# Supabase 連線設定
# Dashboard → Project Settings → API
#   Project URL   → VITE_SUPABASE_URL
#   anon public   → VITE_SUPABASE_ANON_KEY
#
# 複製本檔為 .env 後填入實際值。.env 不進版控。
# 部署到 Vercel 時改在 Project Settings → Environment Variables 設定同名變數。
#
# 注意：VITE_ 開頭的變數會在 build 時內嵌進前端產物，瀏覽器可見。
# 這裡只放 publishable (anon) key，真正的存取控制靠 RLS。
# service_role key 絕對不可放進這個檔案。
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-PUBLISHABLE-KEY

# E2E 測試帳號
# RLS 限 authenticated，測試跑任何流程前要先登入（見 tests/helpers.js 的 signIn）。
# 帳號需先在 Supabase → Authentication 建立，建議另開一組測試專用、不要用日常登入的。
#
# 注意：這兩個變數刻意不加 VITE_ 前綴——加了會在 build 時內嵌進 dist，
# 密碼等於直接公開在瀏覽器裡。
TEST_EMAIL=
TEST_PASSWORD=
